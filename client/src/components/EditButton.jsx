function EditButton({onClick}) {
  return (
    <button className="analiz-btn" onClick={onClick}>
      <span className="analiz-btn-text">Edit Rule</span>
    </button>
  );
}


export default EditButton;
