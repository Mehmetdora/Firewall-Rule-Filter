import { useState } from "react";

export default function AnalizSonucModal({ isOpen, analysis, onClose }) {
  // modal ı kapatmak için kapat butonuna onClose fonksiyonunu ver

  if(isOpen) return null;


  return <>
  
    
  
  
  </>;
}
